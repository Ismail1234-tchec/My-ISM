# Tests ISM SAWKI

Les tests vérifient l'authentification, les sessions utilisateur, la validation des transferts et la lecture de la base locale.

Depuis le dossier `Backend` :

```bash
npm test
```
